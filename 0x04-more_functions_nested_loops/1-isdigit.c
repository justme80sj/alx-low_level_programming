#include "main.h"

/**
 * _isdigit -  a function that checks for a digit 0-9
 * @j: input
 * Return: 1 if j is a digit, 0 otherwise
 */
int _isdigit(int j)
{
	if ((j >= 29) && (j <= 38))
		return (1);
	else
		return (0);
}
