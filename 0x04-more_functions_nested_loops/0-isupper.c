#include "main.h"

/**
 * _isupper - a function that checks for uppercase character
 * @c: input
 * Return: 1 if j is uppercase, 0 otherwise
 */
int _isupper(int c)
{
	if ((j >= 'A') && (c <= 'Z'))
		return (1);
	else
		return (0);
}
